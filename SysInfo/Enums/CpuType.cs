namespace SysInfo.Enums
{
    public enum CpuType
    {
        Other = 1,
        Unknown,
        CentralProcessor,
        MathProcessor,
        DigitalSignalProcessor,
        VideoProcessor
    }
}
